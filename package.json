{
  "name": "rbg-merch-backend",
  "version": "1.0.0",
  "description": "API Backend untuk Web RBG Merch",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "firebase-admin": "^12.0.0"
  }
}